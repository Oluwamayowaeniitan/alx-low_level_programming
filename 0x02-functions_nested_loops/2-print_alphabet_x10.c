#include <stdio.h>
#include "main.h"
/**
 * print_alphabet_x10 - Entry point
 *
 * Return: Always null
 */
void print_alphabet_x10(void)
{
	char (low);
		
		for (low = 'a'; low <= 'z'; low++)
		{
			putchar(low "\n" low "\n" low "\n" low "\n" low "\n" low "\n" low "\n" low "\n" low "\n" low);
		}
	putchar("\n");
}

